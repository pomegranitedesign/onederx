import React from "react";
import styled from "styled-components";

const PNL = props => {
	return (
		<Wrapper>
			<h1>Hello World</h1>
		</Wrapper>
	);
};

// Styled components
const Wrapper = styled.div``;

export default PNL;
